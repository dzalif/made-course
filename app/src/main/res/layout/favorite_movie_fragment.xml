<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    tools:context=".ui.favorite.movie.FavoriteMovieFragment">

    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/rvFavoriteMovie"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        tools:listitem="@layout/item_movie"
        app:layoutManager="androidx.recyclerview.widget.GridLayoutManager"
        app:spanCount="2">
    </androidx.recyclerview.widget.RecyclerView>

    <TextView
        android:id="@+id/textEmptyData"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="@string/no_data"
        android:visibility="gone"
        app:layout_constraintBottom_toBottomOf="@+id/rvFavoriteMovie"
        app:layout_constraintEnd_toEndOf="@+id/rvFavoriteMovie"
        app:layout_constraintStart_toStartOf="@+id/rvFavoriteMovie"
        app:layout_constraintTop_toTopOf="@+id/rvFavoriteMovie" />

</androidx.constraintlayout.widget.ConstraintLayout>